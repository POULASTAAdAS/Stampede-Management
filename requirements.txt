# Enhanced Crowd Monitoring System Dependencies

# Core dependencies
opencv-python>=4.8.0,<5.0.0
# Keep NumPy compatible with the Python version used by the local venv.
# Python 3.9 cannot install NumPy 2.1+; Python 3.10 cannot install 2.3+.
numpy>=2.0.0,<2.1.0; python_version < "3.10"
numpy>=2.2.0,<2.3.0; python_version >= "3.10" and python_version < "3.11"
numpy>=2.3.0,<3.0.0; python_version >= "3.11"
ultralytics>=8.0.0
shapely>=2.0.0

# WebSocket sender (stream data to Spring Boot backend)
websocket-client>=1.6.0

# Optional: For DeepSort tracking
deep-sort-realtime>=1.3.0
